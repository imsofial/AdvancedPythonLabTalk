# Lab
- In the file `deadlock_empire_lab.ipynb` you can see each level task from https://deadlockempire.github.io/ website. One cell contains one level and each section of notebook corresponds to one section of Deadlock Empire. Between cells sometimes neccessary to upsate kernel to kill all remaining threads. In latest cells that problem already solved.

# Talk
- In the file `advanced_python_graph_pipeline_benchmark (3).ipynb` you can find code for **talk benchmarks** for the talk pre-defence. 
- In the file `graph_pipeline_benchmark_results.csv` you can find saved results for benchmarks of talk pre-defence.

- In the file `talk_benchs.ipynb` you can find code for talk. Comparison of performance of Pandas, Polars, NumPy, chDB, Kuzu on 5 tasks usual for data processing.
- In the file `benchmark_results.csv` you can find results of comparison benchmarks. 

- `Graph Data Processing Pipeline.pdf` is presentation for talk defence. Contains theoretical and practival parts 
